- restConfiguration:
    component: platform-http
    scheme: http
    host: localhost
    port: '8080'
- rest:
    id: rest-52ea
    get:
      - id: get-aad2
        path: /api/reservations
        produces: application/json
        to: direct:HandleMessage
- route:
    id: route-0ebf
    from:
      uri: kamelet:oracle
      id: from-da77
      parameters:
        serverName: cs11-dev-db-app-dev.c3ymwt3hbcwm.us-east-2.rds.amazonaws.com
        databaseName: carriersync
        password: d76cbf7
        username: ORADEV
        query: select * from carrier_account
      steps:
        - log:
            message: ${body}
            id: log-3334
