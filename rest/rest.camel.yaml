- route:
    id: route-d1f8
    from:
      uri: direct:ForwardRestData
      id: from-a2d3
      steps:
        - log:
            message: ${body}
            id: log-b8d7
- rest:
    id: rest-cb69
    get:
      - id: get-ef63
        consumes: application/json
        path: /api/reservations
    bindingMode: json
