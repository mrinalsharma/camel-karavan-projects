- rest:
    id: rest-cb69
    post:
      - id: post-924c
        param:
          - {}
        produces: application/json
        consumes: application/json
        bindingMode: json
        path: /postdata/
        to: direct:ForwardRestData
    bindingMode: json
- route:
    id: route-d1f8
    from:
      uri: direct:ForwardRestData
      id: from-a2d3
      steps:
        - to:
            uri: language:js:e
            id: to-a9c8
            parameters:
              script: result = body * 2 + 1
        - log:
            message: ${body}
            id: log-2ac1
